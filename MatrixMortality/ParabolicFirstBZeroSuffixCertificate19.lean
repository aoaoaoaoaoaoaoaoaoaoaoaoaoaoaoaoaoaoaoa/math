import MatrixMortality.ParabolicFirstBZeroCore

/-!
# Leading-`b` suffix certificate shard 19

This file is generated by
`scripts/generate-parabolic-first-b-zero-suffix.py`.
-/

namespace MatrixMortality.ParabolicBlade

private abbrev SCore := FirstBOneOuterSuffixCore
private abbrev killN := firstBOneOuterSuffixCore_false_of_nonpositive
private abbrev killG := firstBOneOuterSuffixCore_false_of_global
private abbrev killK := firstBOneOuterSuffixCore_false_of_gap
private abbrev kill0 := firstBOneOuterSuffixCore_false_of_nil
private abbrev stepB := firstBOneOuterSuffixCore_cons_b
private abbrev stepC := firstBOneOuterSuffixCore_cons_c
private abbrev hRoot := firstBZeroSuffixH
private abbrev aRoot := firstBZeroScaleCoefficient
private abbrev corr := firstBOneOuterCorrection
private abbrev qRoot := firstBOneOuterQ
private abbrev jRoot := firstBOneOuterJ

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8224_1_1
    (body : List TagLetter) (z : Nat) (z_lower : 1 ≤ z)
    (z_upper : z ≤ 1)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8224 z)
      (firstBOneOuterJ 203 8224 z)
      (firstBOneOuterCorrection 203 8224 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8224 z
  let J : ℤ := jRoot 203 8224 z
  let B : ℤ := corr 203 8224 z
  change SCore body H_root J B at core
  have z_lower_int : (1 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 1 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  apply killK 0 body H_root J B
    J_positive B_positive
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · exact core

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8225_1_1
    (body : List TagLetter) (z : Nat) (z_lower : 1 ≤ z)
    (z_upper : z ≤ 1)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8225 z)
      (firstBOneOuterJ 203 8225 z)
      (firstBOneOuterCorrection 203 8225 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8225 z
  let J : ℤ := jRoot 203 8225 z
  let B : ℤ := corr 203 8225 z
  change SCore body H_root J B at core
  have z_lower_int : (1 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 1 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  apply killK 0 body H_root J B
    J_positive B_positive
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · exact core

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8226_1_1
    (body : List TagLetter) (z : Nat) (z_lower : 1 ≤ z)
    (z_upper : z ≤ 1)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8226 z)
      (firstBOneOuterJ 203 8226 z)
      (firstBOneOuterCorrection 203 8226 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8226 z
  let J : ℤ := jRoot 203 8226 z
  let B : ℤ := corr 203 8226 z
  change SCore body H_root J B at core
  have z_lower_int : (1 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 1 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  apply killK 0 body H_root J B
    J_positive B_positive
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · exact core

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8227_1_1
    (body : List TagLetter) (z : Nat) (z_lower : 1 ≤ z)
    (z_upper : z ≤ 1)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8227 z)
      (firstBOneOuterJ 203 8227 z)
      (firstBOneOuterCorrection 203 8227 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8227 z
  let J : ℤ := jRoot 203 8227 z
  let B : ℤ := corr 203 8227 z
  change SCore body H_root J B at core
  have z_lower_int : (1 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 1 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killG tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          apply killG tail1 H_c J B
            J_positive B_positive
          · norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_c

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_2_8383_0_0
    (body : List TagLetter) (z : Nat) (z_lower : 0 ≤ z)
    (z_upper : z ≤ 0)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 2 203 8383 z)
      (firstBOneOuterJ 203 8383 z)
      (firstBOneOuterCorrection 203 8383 z)) : False := by
  let H_root : ℤ := hRoot 2 203 8383 z
  let J : ℤ := jRoot 203 8383 z
  let B : ℤ := corr 203 8383 z
  change SCore body H_root J B at core
  have z_lower_int : (0 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 0 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  apply killK 0 body H_root J B
    J_positive B_positive
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · exact core

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_1_8713_0_0
    (body : List TagLetter) (z : Nat) (z_lower : 0 ≤ z)
    (z_upper : z ≤ 0)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 1 203 8713 z)
      (firstBOneOuterJ 203 8713 z)
      (firstBOneOuterCorrection 203 8713 z)) : False := by
  let H_root : ℤ := hRoot 1 203 8713 z
  let J : ℤ := jRoot 203 8713 z
  let B : ℤ := corr 203 8713 z
  change SCore body H_root J B at core
  have z_lower_int : (0 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 0 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  apply killK 0 body H_root J B
    J_positive B_positive
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · exact core

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_1_8714_0_0
    (body : List TagLetter) (z : Nat) (z_lower : 0 ≤ z)
    (z_upper : z ≤ 0)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 1 203 8714 z)
      (firstBOneOuterJ 203 8714 z)
      (firstBOneOuterCorrection 203 8714 z)) : False := by
  let H_root : ℤ := hRoot 1 203 8714 z
  let J : ℤ := jRoot 203 8714 z
  let B : ℤ := corr 203 8714 z
  change SCore body H_root J B at core
  have z_lower_int : (0 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 0 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  apply killK 0 body H_root J B
    J_positive B_positive
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · exact core

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_9872_0_0
    (body : List TagLetter) (z : Nat) (z_lower : 0 ≤ z)
    (z_upper : z ≤ 0)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 9872 z)
      (firstBOneOuterJ 203 9872 z)
      (firstBOneOuterCorrection 203 9872 z)) : False := by
  let H_root : ℤ := hRoot 0 203 9872 z
  let J : ℤ := jRoot 203 9872 z
  let B : ℤ := corr 203 9872 z
  change SCore body H_root J B at core
  have z_lower_int : (0 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 0 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  apply killN tail2 H_cb J B
                    J_positive B_positive
                  · norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cb
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  apply killK 1 tail2 H_cc J B
                    J_positive B_positive
                  · norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cc

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_9873_0_0
    (body : List TagLetter) (z : Nat) (z_lower : 0 ≤ z)
    (z_upper : z ≤ 0)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 9873 z)
      (firstBOneOuterJ 203 9873 z)
      (firstBOneOuterCorrection 203 9873 z)) : False := by
  let H_root : ℤ := hRoot 0 203 9873 z
  let J : ℤ := jRoot 203 9873 z
  let B : ℤ := corr 203 9873 z
  change SCore body H_root J B at core
  have z_lower_int : (0 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 0 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          apply killK 0 tail1 H_c J B
            J_positive B_positive
          · norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_c

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_9874_0_0
    (body : List TagLetter) (z : Nat) (z_lower : 0 ≤ z)
    (z_upper : z ≤ 0)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 9874 z)
      (firstBOneOuterJ 203 9874 z)
      (firstBOneOuterCorrection 203 9874 z)) : False := by
  let H_root : ℤ := hRoot 0 203 9874 z
  let J : ℤ := jRoot 203 9874 z
  let B : ℤ := corr 203 9874 z
  change SCore body H_root J B at core
  have z_lower_int : (0 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 0 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  apply killK 1 body H_root J B
    J_positive B_positive
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · exact core

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_9875_0_0
    (body : List TagLetter) (z : Nat) (z_lower : 0 ≤ z)
    (z_upper : z ≤ 0)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 9875 z)
      (firstBOneOuterJ 203 9875 z)
      (firstBOneOuterCorrection 203 9875 z)) : False := by
  let H_root : ℤ := hRoot 0 203 9875 z
  let J : ℤ := jRoot 203 9875 z
  let B : ℤ := corr 203 9875 z
  change SCore body H_root J B at core
  have z_lower_int : (0 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 0 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          apply killG tail1 H_c J B
            J_positive B_positive
          · norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_c

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_9876_0_0
    (body : List TagLetter) (z : Nat) (z_lower : 0 ≤ z)
    (z_upper : z ≤ 0)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 9876 z)
      (firstBOneOuterJ 203 9876 z)
      (firstBOneOuterCorrection 203 9876 z)) : False := by
  let H_root : ℤ := hRoot 0 203 9876 z
  let J : ℤ := jRoot 203 9876 z
  let B : ℤ := corr 203 9876 z
  change SCore body H_root J B at core
  have z_lower_int : (0 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 0 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  apply killK 0 body H_root J B
    J_positive B_positive
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · exact core

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_9877_0_0
    (body : List TagLetter) (z : Nat) (z_lower : 0 ≤ z)
    (z_upper : z ≤ 0)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 9877 z)
      (firstBOneOuterJ 203 9877 z)
      (firstBOneOuterCorrection 203 9877 z)) : False := by
  let H_root : ℤ := hRoot 0 203 9877 z
  let J : ℤ := jRoot 203 9877 z
  let B : ℤ := corr 203 9877 z
  change SCore body H_root J B at core
  have z_lower_int : (0 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 0 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  apply killK 0 body H_root J B
    J_positive B_positive
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · exact core

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_9878_0_0
    (body : List TagLetter) (z : Nat) (z_lower : 0 ≤ z)
    (z_upper : z ≤ 0)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 9878 z)
      (firstBOneOuterJ 203 9878 z)
      (firstBOneOuterCorrection 203 9878 z)) : False := by
  let H_root : ℤ := hRoot 0 203 9878 z
  let J : ℤ := jRoot 203 9878 z
  let B : ℤ := corr 203 9878 z
  change SCore body H_root J B at core
  have z_lower_int : (0 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 0 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  apply killK 0 body H_root J B
    J_positive B_positive
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · exact core

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_9879_0_0
    (body : List TagLetter) (z : Nat) (z_lower : 0 ≤ z)
    (z_upper : z ≤ 0)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 9879 z)
      (firstBOneOuterJ 203 9879 z)
      (firstBOneOuterCorrection 203 9879 z)) : False := by
  let H_root : ℤ := hRoot 0 203 9879 z
  let J : ℤ := jRoot 203 9879 z
  let B : ℤ := corr 203 9879 z
  change SCore body H_root J B at core
  have z_lower_int : (0 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 0 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  apply killK 0 body H_root J B
    J_positive B_positive
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
    nlinarith
  · exact core

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_9880_0_0
    (body : List TagLetter) (z : Nat) (z_lower : 0 ≤ z)
    (z_upper : z ≤ 0)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 9880 z)
      (firstBOneOuterJ 203 9880 z)
      (firstBOneOuterCorrection 203 9880 z)) : False := by
  let H_root : ℤ := hRoot 0 203 9880 z
  let J : ℤ := jRoot 203 9880 z
  let B : ℤ := corr 203 9880 z
  change SCore body H_root J B at core
  have z_lower_int : (0 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 0 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killG tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          apply killG tail1 H_c J B
            J_positive B_positive
          · norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_c

/-- Kernel-rechecked suffix extinction on one generated inner-wait ray. -/
theorem firstBZeroSuffix_false_201_0_1036_ray_40
    (body : List TagLetter) (z : Nat) (z_lower : 40 ≤ z)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 201 1036 z)
      (firstBOneOuterJ 201 1036 z)
      (firstBOneOuterCorrection 201 1036 z)) : False := by
  let H_root : ℤ := hRoot 0 201 1036 z
  let J : ℤ := jRoot 201 1036 z
  let B : ℤ := corr 201 1036 z
  change SCore body H_root J B at core
  have z_lower_int : (40 : ℤ) ≤ z := by exact_mod_cast z_lower
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  apply killG tail2 H_cb J B
                    J_positive B_positive
                  · norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cb
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  apply killG tail2 H_cc J B
                    J_positive B_positive
                  · norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cc

/-- Kernel-rechecked suffix extinction on one generated inner-wait ray. -/
theorem firstBZeroSuffix_false_202_0_1844_ray_39
    (body : List TagLetter) (z : Nat) (z_lower : 39 ≤ z)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 202 1844 z)
      (firstBOneOuterJ 202 1844 z)
      (firstBOneOuterCorrection 202 1844 z)) : False := by
  let H_root : ℤ := hRoot 0 202 1844 z
  let J : ℤ := jRoot 202 1844 z
  let B : ℤ := corr 202 1844 z
  change SCore body H_root J B at core
  have z_lower_int : (39 : ℤ) ≤ z := by exact_mod_cast z_lower
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  apply killG tail2 H_cb J B
                    J_positive B_positive
                  · norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cb
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  apply killG tail2 H_cc J B
                    J_positive B_positive
                  · norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cc

/-- Kernel-rechecked suffix extinction on one generated inner-wait ray. -/
theorem firstBZeroSuffix_false_203_4_6962_ray_5966
    (body : List TagLetter) (z : Nat) (z_lower : 5966 ≤ z)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 4 203 6962 z)
      (firstBOneOuterJ 203 6962 z)
      (firstBOneOuterCorrection 203 6962 z)) : False := by
  let H_root : ℤ := hRoot 4 203 6962 z
  let J : ℤ := jRoot 203 6962 z
  let B : ℤ := corr 203 6962 z
  change SCore body H_root J B at core
  have z_lower_int : (5966 : ℤ) ≤ z := by exact_mod_cast z_lower
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  cases tail2 with
                  | nil =>
                      apply kill0 H_cb J B
                      · intro equality
                        norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at
                          equality
                        nlinarith
                      · exact core_cb
                  | cons letter tail3 =>
                      cases letter with
                      | b =>
                          let H_cbb : ℤ := 243 * H_cb - 39 * J
                          have core_cbb : SCore tail3 H_cbb J B := by
                            dsimp [H_cbb]
                            exact stepB tail3 H_cb J B core_cb
                          apply killG tail3 H_cbb J B
                            J_positive B_positive
                          · norm_num [H_cbb, H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot,
                            jRoot]
                            nlinarith
                          · exact core_cbb
                      | c =>
                          let H_cbc : ℤ := 3 * H_cb
                          have core_cbc : SCore tail3 H_cbc J B := by
                            dsimp [H_cbc]
                            exact stepC tail3 H_cb J B core_cb
                          apply killG tail3 H_cbc J B
                            J_positive B_positive
                          · norm_num [H_cbc, H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot,
                            jRoot]
                            nlinarith
                          · exact core_cbc
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  apply killG tail2 H_cc J B
                    J_positive B_positive
                  · norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cc

/-- Kernel-rechecked suffix extinction on one generated inner-wait ray. -/
theorem firstBZeroSuffix_false_203_1_7295_ray_573
    (body : List TagLetter) (z : Nat) (z_lower : 573 ≤ z)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 1 203 7295 z)
      (firstBOneOuterJ 203 7295 z)
      (firstBOneOuterCorrection 203 7295 z)) : False := by
  let H_root : ℤ := hRoot 1 203 7295 z
  let J : ℤ := jRoot 203 7295 z
  let B : ℤ := corr 203 7295 z
  change SCore body H_root J B at core
  have z_lower_int : (573 : ℤ) ≤ z := by exact_mod_cast z_lower
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  apply killN tail2 H_cb J B
                    J_positive B_positive
                  · norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cb
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  cases tail2 with
                  | nil =>
                      apply kill0 H_cc J B
                      · intro equality
                        norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at
                          equality
                        nlinarith
                      · exact core_cc
                  | cons letter tail3 =>
                      cases letter with
                      | b =>
                          let H_ccb : ℤ := 243 * H_cc - 39 * J
                          have core_ccb : SCore tail3 H_ccb J B := by
                            dsimp [H_ccb]
                            exact stepB tail3 H_cc J B core_cc
                          apply killG tail3 H_ccb J B
                            J_positive B_positive
                          · norm_num [H_ccb, H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot,
                            jRoot]
                            nlinarith
                          · exact core_ccb
                      | c =>
                          let H_ccc : ℤ := 3 * H_cc
                          have core_ccc : SCore tail3 H_ccc J B := by
                            dsimp [H_ccc]
                            exact stepC tail3 H_cc J B core_cc
                          apply killG tail3 H_ccc J B
                            J_positive B_positive
                          · norm_num [H_ccc, H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot,
                            jRoot]
                            nlinarith
                          · exact core_ccc

/-- Kernel-rechecked suffix extinction on one generated inner-wait ray. -/
theorem firstBZeroSuffix_false_203_1_7296_ray_129
    (body : List TagLetter) (z : Nat) (z_lower : 129 ≤ z)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 1 203 7296 z)
      (firstBOneOuterJ 203 7296 z)
      (firstBOneOuterCorrection 203 7296 z)) : False := by
  let H_root : ℤ := hRoot 1 203 7296 z
  let J : ℤ := jRoot 203 7296 z
  let B : ℤ := corr 203 7296 z
  change SCore body H_root J B at core
  have z_lower_int : (129 : ℤ) ≤ z := by exact_mod_cast z_lower
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  apply killG tail2 H_cb J B
                    J_positive B_positive
                  · norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cb
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  apply killG tail2 H_cc J B
                    J_positive B_positive
                  · norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cc

/-- Kernel-rechecked suffix extinction on one generated inner-wait ray. -/
theorem firstBZeroSuffix_false_203_0_8095_ray_3009
    (body : List TagLetter) (z : Nat) (z_lower : 3009 ≤ z)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8095 z)
      (firstBOneOuterJ 203 8095 z)
      (firstBOneOuterCorrection 203 8095 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8095 z
  let J : ℤ := jRoot 203 8095 z
  let B : ℤ := corr 203 8095 z
  change SCore body H_root J B at core
  have z_lower_int : (3009 : ℤ) ≤ z := by exact_mod_cast z_lower
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  apply killN tail2 H_cb J B
                    J_positive B_positive
                  · norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cb
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  cases tail2 with
                  | nil =>
                      apply kill0 H_cc J B
                      · intro equality
                        norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at
                          equality
                        nlinarith
                      · exact core_cc
                  | cons letter tail3 =>
                      cases letter with
                      | b =>
                          let H_ccb : ℤ := 243 * H_cc - 39 * J
                          have core_ccb : SCore tail3 H_ccb J B := by
                            dsimp [H_ccb]
                            exact stepB tail3 H_cc J B core_cc
                          apply killN tail3 H_ccb J B
                            J_positive B_positive
                          · norm_num [H_ccb, H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot,
                            jRoot]
                            nlinarith
                          · exact core_ccb
                      | c =>
                          let H_ccc : ℤ := 3 * H_cc
                          have core_ccc : SCore tail3 H_ccc J B := by
                            dsimp [H_ccc]
                            exact stepC tail3 H_cc J B core_cc
                          cases tail3 with
                          | nil =>
                              apply kill0 H_ccc J B
                              · intro equality
                                norm_num [H_ccc, H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot,
                                  jRoot] at equality
                                nlinarith
                              · exact core_ccc
                          | cons letter tail4 =>
                              cases letter with
                              | b =>
                                  let H_cccb : ℤ := 243 * H_ccc - 39 * J
                                  have core_cccb : SCore tail4 H_cccb J B := by
                                    dsimp [H_cccb]
                                    exact stepB tail4 H_ccc J B core_ccc
                                  cases tail4 with
                                  | nil =>
                                      apply kill0 H_cccb J B
                                      · intro equality
                                        norm_num [H_cccb, H_ccc, H_cc, H_c, B, J, H_root, hRoot,
                                          aRoot, corr, qRoot, jRoot] at equality
                                        nlinarith
                                      · exact core_cccb
                                  | cons letter tail5 =>
                                      cases letter with
                                      | b =>
                                          let H_cccbb : ℤ := 243 * H_cccb - 39 * J
                                          have core_cccbb : SCore tail5 H_cccbb J B := by
                                            dsimp [H_cccbb]
                                            exact stepB tail5 H_cccb J B core_cccb
                                          apply killG tail5 H_cccbb J B
                                            J_positive B_positive
                                          · norm_num [H_cccbb, H_cccb, H_ccc, H_cc, H_c, B, J,
                                            H_root, hRoot, aRoot, corr, qRoot, jRoot]
                                            nlinarith
                                          · exact core_cccbb
                                      | c =>
                                          let H_cccbc : ℤ := 3 * H_cccb
                                          have core_cccbc : SCore tail5 H_cccbc J B := by
                                            dsimp [H_cccbc]
                                            exact stepC tail5 H_cccb J B core_cccb
                                          apply killG tail5 H_cccbc J B
                                            J_positive B_positive
                                          · norm_num [H_cccbc, H_cccb, H_ccc, H_cc, H_c, B, J,
                                            H_root, hRoot, aRoot, corr, qRoot, jRoot]
                                            nlinarith
                                          · exact core_cccbc
                              | c =>
                                  let H_cccc : ℤ := 3 * H_ccc
                                  have core_cccc : SCore tail4 H_cccc J B := by
                                    dsimp [H_cccc]
                                    exact stepC tail4 H_ccc J B core_ccc
                                  apply killG tail4 H_cccc J B
                                    J_positive B_positive
                                  · norm_num [H_cccc, H_ccc, H_cc, H_c, B, J, H_root, hRoot, aRoot,
                                    corr, qRoot, jRoot]
                                    nlinarith
                                  · exact core_cccc

/-- Kernel-rechecked suffix extinction on one generated inner-wait ray. -/
theorem firstBZeroSuffix_false_203_0_8096_ray_214
    (body : List TagLetter) (z : Nat) (z_lower : 214 ≤ z)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8096 z)
      (firstBOneOuterJ 203 8096 z)
      (firstBOneOuterCorrection 203 8096 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8096 z
  let J : ℤ := jRoot 203 8096 z
  let B : ℤ := corr 203 8096 z
  change SCore body H_root J B at core
  have z_lower_int : (214 : ℤ) ≤ z := by exact_mod_cast z_lower
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  apply killN tail2 H_cb J B
                    J_positive B_positive
                  · norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cb
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  cases tail2 with
                  | nil =>
                      apply kill0 H_cc J B
                      · intro equality
                        norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at
                          equality
                        nlinarith
                      · exact core_cc
                  | cons letter tail3 =>
                      cases letter with
                      | b =>
                          let H_ccb : ℤ := 243 * H_cc - 39 * J
                          have core_ccb : SCore tail3 H_ccb J B := by
                            dsimp [H_ccb]
                            exact stepB tail3 H_cc J B core_cc
                          apply killG tail3 H_ccb J B
                            J_positive B_positive
                          · norm_num [H_ccb, H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot,
                            jRoot]
                            nlinarith
                          · exact core_ccb
                      | c =>
                          let H_ccc : ℤ := 3 * H_cc
                          have core_ccc : SCore tail3 H_ccc J B := by
                            dsimp [H_ccc]
                            exact stepC tail3 H_cc J B core_cc
                          apply killG tail3 H_ccc J B
                            J_positive B_positive
                          · norm_num [H_ccc, H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot,
                            jRoot]
                            nlinarith
                          · exact core_ccc

/-- Kernel-rechecked suffix extinction on one generated inner-wait ray. -/
theorem firstBZeroSuffix_false_203_0_8097_ray_326
    (body : List TagLetter) (z : Nat) (z_lower : 326 ≤ z)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8097 z)
      (firstBOneOuterJ 203 8097 z)
      (firstBOneOuterCorrection 203 8097 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8097 z
  let J : ℤ := jRoot 203 8097 z
  let B : ℤ := corr 203 8097 z
  change SCore body H_root J B at core
  have z_lower_int : (326 : ℤ) ≤ z := by exact_mod_cast z_lower
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  cases tail2 with
                  | nil =>
                      apply kill0 H_cb J B
                      · intro equality
                        norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at
                          equality
                        nlinarith
                      · exact core_cb
                  | cons letter tail3 =>
                      cases letter with
                      | b =>
                          let H_cbb : ℤ := 243 * H_cb - 39 * J
                          have core_cbb : SCore tail3 H_cbb J B := by
                            dsimp [H_cbb]
                            exact stepB tail3 H_cb J B core_cb
                          apply killG tail3 H_cbb J B
                            J_positive B_positive
                          · norm_num [H_cbb, H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot,
                            jRoot]
                            nlinarith
                          · exact core_cbb
                      | c =>
                          let H_cbc : ℤ := 3 * H_cb
                          have core_cbc : SCore tail3 H_cbc J B := by
                            dsimp [H_cbc]
                            exact stepC tail3 H_cb J B core_cb
                          apply killG tail3 H_cbc J B
                            J_positive B_positive
                          · norm_num [H_cbc, H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot,
                            jRoot]
                            nlinarith
                          · exact core_cbc
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  apply killG tail2 H_cc J B
                    J_positive B_positive
                  · norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cc

end MatrixMortality.ParabolicBlade

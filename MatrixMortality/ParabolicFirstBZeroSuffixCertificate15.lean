import MatrixMortality.ParabolicFirstBZeroCore

/-!
# Leading-`b` suffix certificate shard 15

This file is generated by
`scripts/generate-parabolic-first-b-zero-suffix.py`.
-/

namespace MatrixMortality.ParabolicBlade

private abbrev SCore := FirstBOneOuterSuffixCore
private abbrev killN := firstBOneOuterSuffixCore_false_of_nonpositive
private abbrev killG := firstBOneOuterSuffixCore_false_of_global
private abbrev killK := firstBOneOuterSuffixCore_false_of_gap
private abbrev kill0 := firstBOneOuterSuffixCore_false_of_nil
private abbrev stepB := firstBOneOuterSuffixCore_cons_b
private abbrev stepC := firstBOneOuterSuffixCore_cons_c
private abbrev hRoot := firstBZeroSuffixH
private abbrev aRoot := firstBZeroScaleCoefficient
private abbrev corr := firstBOneOuterCorrection
private abbrev qRoot := firstBOneOuterQ
private abbrev jRoot := firstBOneOuterJ

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8101_22_22
    (body : List TagLetter) (z : Nat) (z_lower : 22 ≤ z)
    (z_upper : z ≤ 22)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8101 z)
      (firstBOneOuterJ 203 8101 z)
      (firstBOneOuterCorrection 203 8101 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8101 z
  let J : ℤ := jRoot 203 8101 z
  let B : ℤ := corr 203 8101 z
  change SCore body H_root J B at core
  have z_lower_int : (22 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 22 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  apply killN tail2 H_cb J B
                    J_positive B_positive
                  · norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cb
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  cases tail2 with
                  | nil =>
                      apply kill0 H_cc J B
                      · intro equality
                        norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at
                          equality
                        nlinarith
                      · exact core_cc
                  | cons letter tail3 =>
                      cases letter with
                      | b =>
                          let H_ccb : ℤ := 243 * H_cc - 39 * J
                          have core_ccb : SCore tail3 H_ccb J B := by
                            dsimp [H_ccb]
                            exact stepB tail3 H_cc J B core_cc
                          apply killN tail3 H_ccb J B
                            J_positive B_positive
                          · norm_num [H_ccb, H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot,
                            jRoot]
                            nlinarith
                          · exact core_ccb
                      | c =>
                          let H_ccc : ℤ := 3 * H_cc
                          have core_ccc : SCore tail3 H_ccc J B := by
                            dsimp [H_ccc]
                            exact stepC tail3 H_cc J B core_cc
                          cases tail3 with
                          | nil =>
                              apply kill0 H_ccc J B
                              · intro equality
                                norm_num [H_ccc, H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot,
                                  jRoot] at equality
                                nlinarith
                              · exact core_ccc
                          | cons letter tail4 =>
                              cases letter with
                              | b =>
                                  let H_cccb : ℤ := 243 * H_ccc - 39 * J
                                  have core_cccb : SCore tail4 H_cccb J B := by
                                    dsimp [H_cccb]
                                    exact stepB tail4 H_ccc J B core_ccc
                                  apply killN tail4 H_cccb J B
                                    J_positive B_positive
                                  · norm_num [H_cccb, H_ccc, H_cc, H_c, B, J, H_root, hRoot, aRoot,
                                    corr, qRoot, jRoot]
                                    nlinarith
                                  · exact core_cccb
                              | c =>
                                  let H_cccc : ℤ := 3 * H_ccc
                                  have core_cccc : SCore tail4 H_cccc J B := by
                                    dsimp [H_cccc]
                                    exact stepC tail4 H_ccc J B core_ccc
                                  apply killG tail4 H_cccc J B
                                    J_positive B_positive
                                  · norm_num [H_cccc, H_ccc, H_cc, H_c, B, J, H_root, hRoot, aRoot,
                                    corr, qRoot, jRoot]
                                    nlinarith
                                  · exact core_cccc

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8101_23_24
    (body : List TagLetter) (z : Nat) (z_lower : 23 ≤ z)
    (z_upper : z ≤ 24)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8101 z)
      (firstBOneOuterJ 203 8101 z)
      (firstBOneOuterCorrection 203 8101 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8101 z
  let J : ℤ := jRoot 203 8101 z
  let B : ℤ := corr 203 8101 z
  change SCore body H_root J B at core
  have z_lower_int : (23 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 24 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  apply killN tail2 H_cb J B
                    J_positive B_positive
                  · norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cb
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  apply killK 0 tail2 H_cc J B
                    J_positive B_positive
                  · norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cc

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8101_25_30
    (body : List TagLetter) (z : Nat) (z_lower : 25 ≤ z)
    (z_upper : z ≤ 30)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8101 z)
      (firstBOneOuterJ 203 8101 z)
      (firstBOneOuterCorrection 203 8101 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8101 z
  let J : ℤ := jRoot 203 8101 z
  let B : ℤ := corr 203 8101 z
  change SCore body H_root J B at core
  have z_lower_int : (25 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 30 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          apply killK 0 tail1 H_c J B
            J_positive B_positive
          · norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_c

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8101_31_174
    (body : List TagLetter) (z : Nat) (z_lower : 31 ≤ z)
    (z_upper : z ≤ 174)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8101 z)
      (firstBOneOuterJ 203 8101 z)
      (firstBOneOuterCorrection 203 8101 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8101 z
  let J : ℤ := jRoot 203 8101 z
  let B : ℤ := corr 203 8101 z
  change SCore body H_root J B at core
  have z_lower_int : (31 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 174 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  apply killG tail2 H_cb J B
                    J_positive B_positive
                  · norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cb
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  apply killG tail2 H_cc J B
                    J_positive B_positive
                  · norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cc

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8101_175_175
    (body : List TagLetter) (z : Nat) (z_lower : 175 ≤ z)
    (z_upper : z ≤ 175)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8101 z)
      (firstBOneOuterJ 203 8101 z)
      (firstBOneOuterCorrection 203 8101 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8101 z
  let J : ℤ := jRoot 203 8101 z
  let B : ℤ := corr 203 8101 z
  change SCore body H_root J B at core
  have z_lower_int : (175 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 175 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_b J B
              · intro equality
                norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_b
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_bb : ℤ := 243 * H_b - 39 * J
                  have core_bb : SCore tail2 H_bb J B := by
                    dsimp [H_bb]
                    exact stepB tail2 H_b J B core_b
                  apply killN tail2 H_bb J B
                    J_positive B_positive
                  · norm_num [H_bb, H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_bb
              | c =>
                  let H_bc : ℤ := 3 * H_b
                  have core_bc : SCore tail2 H_bc J B := by
                    dsimp [H_bc]
                    exact stepC tail2 H_b J B core_b
                  cases tail2 with
                  | nil =>
                      apply kill0 H_bc J B
                      · intro equality
                        norm_num [H_bc, H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at
                          equality
                        nlinarith
                      · exact core_bc
                  | cons letter tail3 =>
                      cases letter with
                      | b =>
                          let H_bcb : ℤ := 243 * H_bc - 39 * J
                          have core_bcb : SCore tail3 H_bcb J B := by
                            dsimp [H_bcb]
                            exact stepB tail3 H_bc J B core_bc
                          apply killN tail3 H_bcb J B
                            J_positive B_positive
                          · norm_num [H_bcb, H_bc, H_b, B, J, H_root, hRoot, aRoot, corr, qRoot,
                            jRoot]
                            nlinarith
                          · exact core_bcb
                      | c =>
                          let H_bcc : ℤ := 3 * H_bc
                          have core_bcc : SCore tail3 H_bcc J B := by
                            dsimp [H_bcc]
                            exact stepC tail3 H_bc J B core_bc
                          cases tail3 with
                          | nil =>
                              apply kill0 H_bcc J B
                              · intro equality
                                norm_num [H_bcc, H_bc, H_b, B, J, H_root, hRoot, aRoot, corr, qRoot,
                                  jRoot] at equality
                                nlinarith
                              · exact core_bcc
                          | cons letter tail4 =>
                              cases letter with
                              | b =>
                                  let H_bccb : ℤ := 243 * H_bcc - 39 * J
                                  have core_bccb : SCore tail4 H_bccb J B := by
                                    dsimp [H_bccb]
                                    exact stepB tail4 H_bcc J B core_bcc
                                  apply killN tail4 H_bccb J B
                                    J_positive B_positive
                                  · norm_num [H_bccb, H_bcc, H_bc, H_b, B, J, H_root, hRoot, aRoot,
                                    corr, qRoot, jRoot]
                                    nlinarith
                                  · exact core_bccb
                              | c =>
                                  let H_bccc : ℤ := 3 * H_bcc
                                  have core_bccc : SCore tail4 H_bccc J B := by
                                    dsimp [H_bccc]
                                    exact stepC tail4 H_bcc J B core_bcc
                                  apply killK 1 tail4 H_bccc J B
                                    J_positive B_positive
                                  · norm_num [H_bccc, H_bcc, H_bc, H_b, B, J, H_root, hRoot, aRoot,
                                    corr, qRoot, jRoot]
                                    nlinarith
                                  · norm_num [H_bccc, H_bcc, H_bc, H_b, B, J, H_root, hRoot, aRoot,
                                    corr, qRoot, jRoot]
                                    nlinarith
                                  · exact core_bccc
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          apply killG tail1 H_c J B
            J_positive B_positive
          · norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_c

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8101_176_176
    (body : List TagLetter) (z : Nat) (z_lower : 176 ≤ z)
    (z_upper : z ≤ 176)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8101 z)
      (firstBOneOuterJ 203 8101 z)
      (firstBOneOuterCorrection 203 8101 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8101 z
  let J : ℤ := jRoot 203 8101 z
  let B : ℤ := corr 203 8101 z
  change SCore body H_root J B at core
  have z_lower_int : (176 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 176 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_b J B
              · intro equality
                norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_b
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_bb : ℤ := 243 * H_b - 39 * J
                  have core_bb : SCore tail2 H_bb J B := by
                    dsimp [H_bb]
                    exact stepB tail2 H_b J B core_b
                  apply killN tail2 H_bb J B
                    J_positive B_positive
                  · norm_num [H_bb, H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_bb
              | c =>
                  let H_bc : ℤ := 3 * H_b
                  have core_bc : SCore tail2 H_bc J B := by
                    dsimp [H_bc]
                    exact stepC tail2 H_b J B core_b
                  apply killK 0 tail2 H_bc J B
                    J_positive B_positive
                  · norm_num [H_bc, H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · norm_num [H_bc, H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_bc
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          apply killG tail1 H_c J B
            J_positive B_positive
          · norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_c

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8101_177_180
    (body : List TagLetter) (z : Nat) (z_lower : 177 ≤ z)
    (z_upper : z ≤ 180)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8101 z)
      (firstBOneOuterJ 203 8101 z)
      (firstBOneOuterCorrection 203 8101 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8101 z
  let J : ℤ := jRoot 203 8101 z
  let B : ℤ := corr 203 8101 z
  change SCore body H_root J B at core
  have z_lower_int : (177 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 180 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_b J B
              · intro equality
                norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_b
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_bb : ℤ := 243 * H_b - 39 * J
                  have core_bb : SCore tail2 H_bb J B := by
                    dsimp [H_bb]
                    exact stepB tail2 H_b J B core_b
                  apply killN tail2 H_bb J B
                    J_positive B_positive
                  · norm_num [H_bb, H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_bb
              | c =>
                  let H_bc : ℤ := 3 * H_b
                  have core_bc : SCore tail2 H_bc J B := by
                    dsimp [H_bc]
                    exact stepC tail2 H_b J B core_b
                  apply killG tail2 H_bc J B
                    J_positive B_positive
                  · norm_num [H_bc, H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_bc
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          apply killG tail1 H_c J B
            J_positive B_positive
          · norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_c

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8101_181_934
    (body : List TagLetter) (z : Nat) (z_lower : 181 ≤ z)
    (z_upper : z ≤ 934)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8101 z)
      (firstBOneOuterJ 203 8101 z)
      (firstBOneOuterCorrection 203 8101 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8101 z
  let J : ℤ := jRoot 203 8101 z
  let B : ℤ := corr 203 8101 z
  change SCore body H_root J B at core
  have z_lower_int : (181 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 934 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killG tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          apply killG tail1 H_c J B
            J_positive B_positive
          · norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_c

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8102_19_19
    (body : List TagLetter) (z : Nat) (z_lower : 19 ≤ z)
    (z_upper : z ≤ 19)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8102 z)
      (firstBOneOuterJ 203 8102 z)
      (firstBOneOuterCorrection 203 8102 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8102 z
  let J : ℤ := jRoot 203 8102 z
  let B : ℤ := corr 203 8102 z
  change SCore body H_root J B at core
  have z_lower_int : (19 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 19 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  apply killN tail2 H_cb J B
                    J_positive B_positive
                  · norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cb
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  cases tail2 with
                  | nil =>
                      apply kill0 H_cc J B
                      · intro equality
                        norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at
                          equality
                        nlinarith
                      · exact core_cc
                  | cons letter tail3 =>
                      cases letter with
                      | b =>
                          let H_ccb : ℤ := 243 * H_cc - 39 * J
                          have core_ccb : SCore tail3 H_ccb J B := by
                            dsimp [H_ccb]
                            exact stepB tail3 H_cc J B core_cc
                          apply killN tail3 H_ccb J B
                            J_positive B_positive
                          · norm_num [H_ccb, H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot,
                            jRoot]
                            nlinarith
                          · exact core_ccb
                      | c =>
                          let H_ccc : ℤ := 3 * H_cc
                          have core_ccc : SCore tail3 H_ccc J B := by
                            dsimp [H_ccc]
                            exact stepC tail3 H_cc J B core_cc
                          apply killK 0 tail3 H_ccc J B
                            J_positive B_positive
                          · norm_num [H_ccc, H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot,
                            jRoot]
                            nlinarith
                          · norm_num [H_ccc, H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot,
                            jRoot]
                            nlinarith
                          · exact core_ccc

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8102_20_20
    (body : List TagLetter) (z : Nat) (z_lower : 20 ≤ z)
    (z_upper : z ≤ 20)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8102 z)
      (firstBOneOuterJ 203 8102 z)
      (firstBOneOuterCorrection 203 8102 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8102 z
  let J : ℤ := jRoot 203 8102 z
  let B : ℤ := corr 203 8102 z
  change SCore body H_root J B at core
  have z_lower_int : (20 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 20 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          apply killK 1 tail1 H_c J B
            J_positive B_positive
          · norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_c

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8102_21_25
    (body : List TagLetter) (z : Nat) (z_lower : 21 ≤ z)
    (z_upper : z ≤ 25)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8102 z)
      (firstBOneOuterJ 203 8102 z)
      (firstBOneOuterCorrection 203 8102 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8102 z
  let J : ℤ := jRoot 203 8102 z
  let B : ℤ := corr 203 8102 z
  change SCore body H_root J B at core
  have z_lower_int : (21 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 25 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  apply killN tail2 H_cb J B
                    J_positive B_positive
                  · norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cb
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  apply killG tail2 H_cc J B
                    J_positive B_positive
                  · norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cc

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8102_26_76
    (body : List TagLetter) (z : Nat) (z_lower : 26 ≤ z)
    (z_upper : z ≤ 76)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8102 z)
      (firstBOneOuterJ 203 8102 z)
      (firstBOneOuterCorrection 203 8102 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8102 z
  let J : ℤ := jRoot 203 8102 z
  let B : ℤ := corr 203 8102 z
  change SCore body H_root J B at core
  have z_lower_int : (26 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 76 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  apply killG tail2 H_cb J B
                    J_positive B_positive
                  · norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cb
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  apply killG tail2 H_cc J B
                    J_positive B_positive
                  · norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cc

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8102_77_77
    (body : List TagLetter) (z : Nat) (z_lower : 77 ≤ z)
    (z_upper : z ≤ 77)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8102 z)
      (firstBOneOuterJ 203 8102 z)
      (firstBOneOuterCorrection 203 8102 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8102 z
  let J : ℤ := jRoot 203 8102 z
  let B : ℤ := corr 203 8102 z
  change SCore body H_root J B at core
  have z_lower_int : (77 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 77 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_b J B
              · intro equality
                norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_b
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_bb : ℤ := 243 * H_b - 39 * J
                  have core_bb : SCore tail2 H_bb J B := by
                    dsimp [H_bb]
                    exact stepB tail2 H_b J B core_b
                  apply killN tail2 H_bb J B
                    J_positive B_positive
                  · norm_num [H_bb, H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_bb
              | c =>
                  let H_bc : ℤ := 3 * H_b
                  have core_bc : SCore tail2 H_bc J B := by
                    dsimp [H_bc]
                    exact stepC tail2 H_b J B core_b
                  apply killG tail2 H_bc J B
                    J_positive B_positive
                  · norm_num [H_bc, H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_bc
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          apply killG tail1 H_c J B
            J_positive B_positive
          · norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_c

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8102_78_118
    (body : List TagLetter) (z : Nat) (z_lower : 78 ≤ z)
    (z_upper : z ≤ 118)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8102 z)
      (firstBOneOuterJ 203 8102 z)
      (firstBOneOuterCorrection 203 8102 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8102 z
  let J : ℤ := jRoot 203 8102 z
  let B : ℤ := corr 203 8102 z
  change SCore body H_root J B at core
  have z_lower_int : (78 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 118 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killG tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          apply killG tail1 H_c J B
            J_positive B_positive
          · norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_c

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8103_17_17
    (body : List TagLetter) (z : Nat) (z_lower : 17 ≤ z)
    (z_upper : z ≤ 17)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8103 z)
      (firstBOneOuterJ 203 8103 z)
      (firstBOneOuterCorrection 203 8103 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8103 z
  let J : ℤ := jRoot 203 8103 z
  let B : ℤ := corr 203 8103 z
  change SCore body H_root J B at core
  have z_lower_int : (17 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 17 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  apply killN tail2 H_cb J B
                    J_positive B_positive
                  · norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cb
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  apply killK 0 tail2 H_cc J B
                    J_positive B_positive
                  · norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cc

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8103_18_21
    (body : List TagLetter) (z : Nat) (z_lower : 18 ≤ z)
    (z_upper : z ≤ 21)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8103 z)
      (firstBOneOuterJ 203 8103 z)
      (firstBOneOuterCorrection 203 8103 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8103 z
  let J : ℤ := jRoot 203 8103 z
  let B : ℤ := corr 203 8103 z
  change SCore body H_root J B at core
  have z_lower_int : (18 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 21 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          cases tail1 with
          | nil =>
              apply kill0 H_c J B
              · intro equality
                norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
                nlinarith
              · exact core_c
          | cons letter tail2 =>
              cases letter with
              | b =>
                  let H_cb : ℤ := 243 * H_c - 39 * J
                  have core_cb : SCore tail2 H_cb J B := by
                    dsimp [H_cb]
                    exact stepB tail2 H_c J B core_c
                  apply killN tail2 H_cb J B
                    J_positive B_positive
                  · norm_num [H_cb, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cb
              | c =>
                  let H_cc : ℤ := 3 * H_c
                  have core_cc : SCore tail2 H_cc J B := by
                    dsimp [H_cc]
                    exact stepC tail2 H_c J B core_c
                  apply killG tail2 H_cc J B
                    J_positive B_positive
                  · norm_num [H_cc, H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
                    nlinarith
                  · exact core_cc

/-- Kernel-rechecked suffix extinction on one generated inner-wait interval. -/
theorem firstBZeroSuffix_false_203_0_8103_22_48
    (body : List TagLetter) (z : Nat) (z_lower : 22 ≤ z)
    (z_upper : z ≤ 48)
    (core : FirstBOneOuterSuffixCore body
      (firstBZeroSuffixH 0 203 8103 z)
      (firstBOneOuterJ 203 8103 z)
      (firstBOneOuterCorrection 203 8103 z)) : False := by
  let H_root : ℤ := hRoot 0 203 8103 z
  let J : ℤ := jRoot 203 8103 z
  let B : ℤ := corr 203 8103 z
  change SCore body H_root J B at core
  have z_lower_int : (22 : ℤ) ≤ z := by exact_mod_cast z_lower
  have z_upper_int : (z : ℤ) ≤ 48 := by exact_mod_cast z_upper
  have J_positive : (0 : ℤ) < J := by
    dsimp [J]
    unfold jRoot
    positivity
  have B_positive : (0 : ℤ) < B := by
    dsimp [B]
    unfold corr qRoot
    positivity
  cases body with
  | nil =>
      apply kill0 H_root J B
      · intro equality
        norm_num [B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot] at equality
        nlinarith
      · exact core
  | cons letter tail1 =>
      cases letter with
      | b =>
          let H_b : ℤ := 243 * H_root - 39 * J
          have core_b : SCore tail1 H_b J B := by
            dsimp [H_b]
            exact stepB tail1 H_root J B core
          apply killN tail1 H_b J B
            J_positive B_positive
          · norm_num [H_b, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_b
      | c =>
          let H_c : ℤ := 3 * H_root
          have core_c : SCore tail1 H_c J B := by
            dsimp [H_c]
            exact stepC tail1 H_root J B core
          apply killG tail1 H_c J B
            J_positive B_positive
          · norm_num [H_c, B, J, H_root, hRoot, aRoot, corr, qRoot, jRoot]
            nlinarith
          · exact core_c

end MatrixMortality.ParabolicBlade

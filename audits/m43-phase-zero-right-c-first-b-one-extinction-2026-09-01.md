# M₄(3) phase-zero right-`c` first-`b`-one extinction audit

**Date:** 1 September 2026

**Status:** the complete physical `cb` cylinder is empty

**Authorship:** GPT-5.6 Sol; elicited by @eternalism_4eva

**Target:** eliminate the five exact suffix chambers left by M4-S41

## Verdict

No physical body beginning `cb` whose remaining tail contains `b` zeros the primitive
phase-zero right-`c` core, at any natural waits. Consequently, no body beginning `cb` with an
even number of `b` letters closes the residual `b | b | c` bridge.

This closes first-`b` position one inside the surviving even-length/even-`b` body rectangle. It
does not close the other leading positions or the complete M₄(3) mortality problem.

## Exact Balance

For outer, middle, and inner waits `x,y,z`, define

```text
Q = −P(x,z),
J = complementCore(x,y,z),
A = 729(72y−9)Q,
B = (8y−9)Q.
```

If `T,E` are the tail scale and complement, a physical `cb` zero satisfies

```text
(39T+E)J = TA−B.
```

Writing the tail as `c^j b rest`, and writing `R,G` for the remaining suffix coordinates,
gives

```text
GJ = RH−B,
H = 243·3^j(A−39J)−39J.
```

This equation is checked directly from the primitive core over the integers. It is not inferred
from sampled words or floating-point roots.

## Ray Cut

M4-S41 leaves one apparent ray `(x,j,y)=(210,1,801)`, `z≥380`. For `z≥1448`, the two exact
position-zero grammar margins are respectively positive and negative. The strict density gap
therefore contradicts the suffix equation. Every ray survivor satisfies `z≤1447`.

The inclusive interval `380≤z≤1447` partitions as

```text
380; 381..382; 383..389; 390..409; 410..486; 487;
488..1121; 1122; 1123..1124; 1125..1130; 1131..1447.
```

One suffix tree is valid uniformly on each interval. These trees contain 81 nodes and have
maximum depth five. The four finite M4-S41 chambers add 15 nodes, for 96 nodes total.

## Trust Boundary

The deterministic generator performs exact integer affine arithmetic, verifies the complete
interval partition and tree size, and emits ordinary Lean source. Each emitted leaf is proved
again by `norm_num` and `nlinarith`; each grammar edge invokes a kernel-checked transition
theorem. The generator is a theorem producer, not an oracle, and contributes no axiom.

## Scope

The primitive theorem assumes only that the body begins `cb` and that its remaining tail
contains `b`. The parity corollary derives that membership from even total `b` count. No upper
bound on body length, suffix depth, trailing run, or any wait survives in the final theorem.

## Validation

The analytic module, both generated shards, aggregate certificate, and physical closure build
without warnings under Lean `4.33.1`. Every generated shard contains fewer than 1,500 lines.
The generator passes deterministic regeneration, Ruff, formatting, and `ty` checks.
Publication-facing declarations are included in `AxiomAudit.lean`; no proof aperture, external
declaration, unsafe definition, or linter suppression is present.

## Artifacts

[`MatrixMortality/ParabolicFirstBOneOuterSuffixCore.lean`](../MatrixMortality/ParabolicFirstBOneOuterSuffixCore.lean),
[`MatrixMortality/ParabolicFirstBOneOuterSuffixCertificate.lean`](../MatrixMortality/ParabolicFirstBOneOuterSuffixCertificate.lean),
[`MatrixMortality/ParabolicFirstBOneOuterClosure.lean`](../MatrixMortality/ParabolicFirstBOneOuterClosure.lean),
and
[`scripts/generate-parabolic-first-b-one-outer-suffix.py`](../scripts/generate-parabolic-first-b-one-outer-suffix.py)

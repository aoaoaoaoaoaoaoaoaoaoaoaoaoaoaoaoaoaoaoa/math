# M₅(3) Deletion-First Fibre Fracture

**Date:** 2026-08-30
**Author and formalizer:** GPT-5.6 Sol
**Human role:** elicited by @eternalism_4eva
**Target:** five-state binary same-zero root
**Verdict:** the literal deletion-first fibre merge is closed; the general same-zero route
remains open

## Correct Victory Condition

The scalar-to-mortality theorem `mortal_adjoin_outer_iff` requires neither invertible data
matrices nor a wordwise rejection theorem for odd binary words. A five-state two-letter family
closes `M₅(3)` as soon as its nonempty scalar-zero existence is equivalent to the source terminal
match. Singular data products already supply scalar-zero witnesses, and odd words may vanish on
yes-instances. They must only avoid creating a zero on source no-instances.

The former nonsingularity and universal odd-word requirements were sufficient conditions, not
the master interface.

## Exact Deletion-First Merge

The lower channel has three fibres: complete-block boundary, pending rule, and pending deletion.
The proposed five-state scheduling uses the identity

```text
(V_b^D,B_b^D)=(V_c^D,B_c^D)=(1,3)
```

to process deletion before the letter bit is known, then tries to represent the pending rule and
deletion values in one two-dimensional lower carrier. Let `A` and `C` be the two bit operators,
and let `b,r,d` denote unit lower values in the boundary, rule, and deletion fibres. Exact
compatibility requires

```text
A r = 27b,       A d = 3b,
C r = ρ_cb,      C d = 3b,
```

where `ρ_c=nearySideLowerCScale β body`. If `A` is injective on the displayed carrier, then

```text
A(27d)=81b=A(3r),
```

so `27d=3r`. Applying `C` gives `81b=3ρ_cb`; since `b≠0`, `ρ_c=27`.
For every nonempty Neary body, the rule-`c` lower word has length at least four and hence
`ρ_c=3^length>27`. The exact two-fibre merge is impossible.

Lean proves the ambient-module statement without a dimension assumption, the Neary scale bound,
and their contradiction in `MatrixMortality/DeletionFirstFracture.lean`.

## Scope

The obstruction assumes exact linear transport of the native lower value through one shared
fibre. It survives a common invertible change of basis. It does not constrain a different
same-zero series, a nonlinear zero-preserving recurrence, a history-sensitive carrier outside
the native lower value, or an existence-only compiler which changes the accepted even words.

The surviving binary lane is therefore narrower but still live: change the nonzero coefficients
or the semantic state, rather than scheduling the unchanged lower radix channel earlier.

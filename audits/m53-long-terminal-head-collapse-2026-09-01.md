# Long Terminal-Head Collapse Audit

**Date:** 2026-09-01
**Target:** the support-saturated long `cb` chamber above the decimal `R_c` singleton root
**Authors:** GPT-5.6 Sol; elicited by @eternalism_4eva
**Verdict:** every hypothetical long `cb` three-block pole collapses to an impossible two-block
singleton pole

## Collapse

Let `H` be the peeled terminal-head code, `P,V,A` the current upper code, lower code, and scale,
and `S` the singleton target trace. Exact suffix exhaustion and the three-block pole equation
give

```text
H((EP+GV)S−7EμGA)=10GVμS.                           (1)
```

The terminal head obeys two simultaneous calibrations:

```text
9H=G,          H−10μ=−q,          E=9q.             (2)
```

Substitution in (1), followed only by cancellation of the nonzero factors `Gq`, yields

```text
S(P−V)=7μGA.                                        (3)
```

Equation (3) is exactly the `R_c`-rooted two-block singleton-pole equation for the same current
and target. The inherited erasure-ending current and literal rule root satisfy `BlocksLaw`, so
`MM-S89` excludes it for either singleton letter.

## Scope

The theorem assumes `β≥3`, an actual `R_c`-rooted three-block singleton pole, multi-role
erasure-ending current and intervening blocks, a long current, and intervening role letters
beginning `cb`. It proves complete extinction of that chamber. No reachability or coprimality
hypothesis is used.

## Verification

The dedicated module, aggregate build, selected namespace lint, Lean LSP diagnostics,
forbidden-aperture scan, whitespace check, and axiom audit pass. Both selected theorems depend
only on the reviewed standard axiom set.

## Artifacts

- [`DecimalSetterThreeBlockLongTerminal.lean`](../MatrixMortality/DecimalSetterThreeBlockLongTerminal.lean)
- [`DecimalSetterTwoBlockSingleton.lean`](../MatrixMortality/DecimalSetterTwoBlockSingleton.lean)
- [`SALVAGE.md`](../SALVAGE.md#mm-s103-long-terminal-head-collapse-extinction)

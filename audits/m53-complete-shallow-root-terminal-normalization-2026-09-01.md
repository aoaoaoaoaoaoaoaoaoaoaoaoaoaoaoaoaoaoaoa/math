# Complete Shallow-Root Terminal Normalization Audit

**Date:** 2026-09-01
**Target:** the non-singleton shallow branch of the parsed decimal setter frontier
**Authors:** GPT-5.6 Sol; elicited by @eternalism_4eva
**Verdict:** every parser-lawful shallow pole has source exactly `R_c` and is exactly a literal
Neary terminal match

## Target Trace

For `β≥2`, every punctuated upper word ends in the marker's final two zero bits. A target with at
least two roles and an erasure last role also has a lower spelling ending in two zero bits: one
from the preceding nonempty spelling and one from the final erasure. Under the decimal digit map
both codes are therefore `77 (mod 100)`.

The generic calibrated trace theorem turns these suffixes into the exact shell

```text
ν₂(gap·P + lift·V) = ν₅(gap·P + lift·V) = 1.
```

No body-length or tag-word hypothesis enters this step.

## Forced Root

For source upper code `H`, upper length `m`, marker `μ`, and complement
`Δ=μ·10^m−H`, the shallow pole equation is equivalent to

```text
H·(gap·P + lift·V) = lift·μ·10^m·V.
```

The four exposed factors outside the trace and radix power are decimal units. Exact valuation
balance forces `m=1`. Since every physical role emits a nonempty upper word, the parser's
rule-ended source has one role. The singleton classifier excludes `R_b` and leaves `R_c`.

## Terminal Boundary

Over `R_c`, the root code and complement calibrations cancel the pole equation to `P=V`.
Injectivity of the radix-ten code makes this equality equivalent to the literal Neary terminal
word equation. The result preserves lawful halting witnesses and excludes every malformed
shallow pole.

Singleton targets and deeper histories do not satisfy this audit's hypotheses and remain open
outer branches.

## Verification

The dedicated module and root aggregate build without warnings. Default lint and Lean LSP
diagnostics are clean. The selected transitive axiom audit contains only the reviewed standard
axioms. Forbidden-form and diff checks pass; no proof aperture or linter suppression is present.

## Artifacts

- [`DecimalSetterShallow.lean`](../MatrixMortality/DecimalSetterShallow.lean)
- [`DecimalSetterMinimumBody.lean`](../MatrixMortality/DecimalSetterMinimumBody.lean)
- [`DecimalSetterBridge.lean`](../MatrixMortality/DecimalSetterBridge.lean)
- [`SALVAGE.md`](../SALVAGE.md#mm-s84-complete-shallow-root-terminal-normalization)

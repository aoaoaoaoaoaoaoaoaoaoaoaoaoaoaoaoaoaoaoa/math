# Minimum-Body Lawful Shallow Pole Audit

**Date:** 2026-08-31
**Target:** exact attainability boundary for the decimal setter's shallow generalized root
**Authors:** GPT-5.6 Sol; elicited by @eternalism_4eva
**Verdict:** every minimum-length body has one lawful shallow pole and a cleared integer
mortality witness; the primitive-recursive compiler image lies strictly above this slice

## Literal Identity

Assume `β>2` and `|body|=β−1`. Define

```text
source = [R_c],
target = R_c :: body.map D.
```

The target upper spelling is

```text
1 · tagEncode(body),
```

followed by the marker `1·0^β`. Its lower spelling is

```text
1 · tagEncode(body) · 1·0 · 0^|body|.
```

The length hypothesis makes these words literally equal. Hence their decimal codes satisfy
`P=V`; no injectivity, valuation, or bounded search is used.

## Root Calibration

For the one-`R_c` source, direct radix-ten evaluation and the marker identity give

```text
9H = 502·10^β−7 = lift(10^β),
9Δ = 18·10^β−63 = gap(10^β).
```

The `MM-S77` shallow equation then holds by commutativity. Lean constructs the physical
`CoreSpelling`, proves its bridge coefficient is zero, classifies it in the shallow arm of
`HasParsedZeroFrontier`, and obtains mortality of both the rational generator and its exactly
cleared integer `5 × 5` family.

## Lawful Status

This pole is not malformed. At minimum body length the source queue is

```text
body.drop(β−1) · b = [b],
```

whose length is already below `β`. It halts without a tag step. The existing minimum-body
history theorem proves that `target` is the unique role word satisfying the terminal equation;
the present module reuses that canonical owner rather than introducing a second target notion.

## Compiler Separation

For every positive cyclic-tag period, Neary's compiler emits

```text
|body| = (safetyBound·β+1)(β−1).
```

Both factors are positive and the first is greater than one, so Lean proves
`|body|>β−1`. The universal source therefore cannot instantiate this exact minimum-body family.
This separation is only by length; it does not exclude related shallow poles for longer bodies.

## Consequence

A global emptiness theorem for the generalized shallow equation is false. Any valid
projective-avoidance proof must preserve lawful terminal poles while excluding malformed parser
roots, and therefore must use emitted-body structure beyond decimal-unit shells.

## Verification

The new module and root aggregate build without warnings. Dedicated default lint and Lean LSP
diagnostics are clean. The selected transitive axiom audit contains only the reviewed standard
axioms. Forbidden-form and diff checks pass; no proof aperture or linter suppression is present.

## Artifacts

- [`DecimalSetterMinimumBody.lean`](../MatrixMortality/DecimalSetterMinimumBody.lean)
- [`DecimalSetterBridge.lean`](../MatrixMortality/DecimalSetterBridge.lean)
- [`HistoryFracture.lean`](../MatrixMortality/HistoryFracture.lean)
- [`SALVAGE.md`](../SALVAGE.md#mm-s79-minimum-body-lawful-shallow-pole)

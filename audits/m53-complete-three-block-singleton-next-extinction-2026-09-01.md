# Complete Three-Block Singleton-Next Extinction Audit

**Date:** 2026-09-01
**Target:** the current-multi, next-singleton seam left by the three-block classifier
**Authors:** GPT-5.6 Sol; elicited by @eternalism_4eva
**Verdict:** neither `D_b` nor `D_c` can be the intervening singleton; every lawful
three-block singleton pole has two multi-role non-root blocks

## `D_b` Intervener

Every erasure-current singleton pole forces the older projective quotient strictly above one.
The exact ray over `[D_b,root]` lies strictly below one for every physical root. These bounds
are incompatible.

## `D_c` Intervener

The singleton-root ray never admits unit peeled ancestry. Consequently any surviving source
would have a short all-`c` current. The exact mixed-prime recurrence then fails in both root
chambers: a shallow root gives equal numerator depths where the pole requires a one-step gap;
a root of depth at least `β` makes the five-depth too large.

Combining both exclusions with `MM-S95` proves that both current and next are multi-role. The
canonical theorem `singletonPole_threeBlock_classifier` therefore exposes only the established
multi/multi deep-root and `R_c` discrepancy chambers, with no cardinality hypotheses.

## Scope

The result classifies lawful three-block singleton poles under the parser shell and physical
erasure-ending assumptions. It does not eliminate the surviving multi/multi chambers.

## Verification

The module and selected namespace lint compile without warnings. Lean LSP reports no
diagnostics. The forbidden-aperture scan and whitespace check are empty. Each selected theorem
depends only on the reviewed standard axiom set.

## Artifacts

- [`DecimalSetterThreeBlockSingletonNext.lean`](../MatrixMortality/DecimalSetterThreeBlockSingletonNext.lean)
- [`SALVAGE.md`](../SALVAGE.md#mm-s100-complete-three-block-singleton-next-extinction)

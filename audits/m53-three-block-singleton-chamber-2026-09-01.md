# Three-Block Singleton Chamber Audit

**Date:** 2026-09-01
**Target:** the first three-source layer of the decimal setter singleton frontier
**Authors:** GPT-5.6 Sol; elicited by @eternalism_4eva
**Verdict:** the lawful multi/multi layer has an exact deep-root/`R_c` grammar, and every
`R_c`-rooted singleton-current case is impossible

## Positive Chamber

For current block data `(T,V,A)`, singleton trace `S`, and older ray `(x,y)`, a pole obeys

```text
(Tx−lift·Vy)S = gap·μ·lift·A·x·7.                    (1)
```

For every erasure-ended physical current and `β≥3`, Lean proves

```text
T·S − 7·gap·μ·lift·A > 0.                            (2)
```

Parser law makes the older ray nonzero. Equation (1) then forces `x≠0`; solving for `y/x` and
using positivity of `lift`, `V`, and `S` shows that every actual singleton pole has positive
older quotient. This sign theorem is independent of the length and spelling of the older
history.

## Multi/Multi Classifier

Let `k` be the upper length of the intervening multi-role block. Above a root of upper length at
least two, the intervening ray has quotient shell `(k−1,k−1)`. Its unit peeled ancestry is
therefore equivalent to `k=2`. Physical spelling at that length forces exactly two `c` roles.
The singleton ancestry equivalence then puts the current block in the long corridor
`m≥β+3`.

If the root is not deep, its nonempty rule-ended spelling has upper length one and is exactly
`R_c`. Define

```text
δ = upperBoundaryCode(next) − lowerBoundaryCode(next).
```

The exact `R_c` ray is proportional to `(δ,10^k μ)`. Consequently the current is long exactly
when `δ` has shell `(k−1,k−1)`. In the complementary branch, the current is marker-free,
all-`c`, and has width at most `β+2`.

On the long branch, positivity turns `δ` into a positive natural difference. Its exact decimal
shell exhausts the final `k−1` digits: the lower spelling is that suffix of the punctuated upper
spelling, and the remaining prefix is a decimal unit. The encoded peeled-head trichotomy then
excludes leading `b`; the intervening letter word begins `cb` or `cc`.

## Singleton Currents

For current `D_c`, the exact pole equation forces

```text
shell(δ)=(k+β,k+β−1),       older quotient > 10^β/4.
```

The quotient identity bounds `δ` above by forty upper-scale units. Its shell makes it a positive
multiple of at least one hundred upper-scale units, a contradiction.

For current `D_b`, the corresponding exact data are

```text
shell(δ)=(k−1,k−2),         older quotient > 2·10^(2β).
```

The quotient bound gives `δ·10^β<5·10^k`, whereas the shell and `10^β≥1000` force the reverse
strict inequality. Thus neither singleton current reaches either singleton target through an
arbitrary intervening block above `R_c`.

## Boundary

The classifier assumes parser law, `β≥3`, and multi-role current and intervening blocks. It does
not assert reachability of either surviving branch. The singleton-current theorem is complete
over `R_c`; deep-root singleton-current histories and singleton intervening blocks require a
separate root-chamber argument. The deep-root two-`c` arm and the `R_c` `cb/cc` arms also remain
open.

## Verification

All three dedicated modules and the root aggregate build without warnings. Default namespace
lint, the axiom audit, forbidden-aperture scans, and whitespace checks pass. Every selected
declaration depends only on the reviewed standard axiom set; there is no project axiom,
admission, unsafe definition, external declaration, or linter suppression.

## Artifacts

- [`DecimalSetterSingletonChamber.lean`](../MatrixMortality/DecimalSetterSingletonChamber.lean)
- [`DecimalSetterThreeBlockSingleton.lean`](../MatrixMortality/DecimalSetterThreeBlockSingleton.lean)
- [`DecimalSetterThreeBlockSingletonCurrent.lean`](../MatrixMortality/DecimalSetterThreeBlockSingletonCurrent.lean)
- [`SALVAGE.md`](../SALVAGE.md#mm-s94-three-block-singleton-chamber-classification)

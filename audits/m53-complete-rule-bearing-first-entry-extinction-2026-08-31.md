# Complete Rule-Bearing First-Entry Extinction Audit

**Date:** 2026-08-31
**Target:** three `5 × 5` integer matrices
**Authors:** GPT-5.6 Sol; elicited by @eternalism_4eva
**Verdict:** no rule-bearing block reaches the physical multi-role pole shell from a lawful
two-`c` raw head

## Alphabet Split

Factor the block at its rightmost rule. The letters on the preceding roles, the rule, and its
erasure tail form a word over `{b,c}`. If `b` occurs, MM-S60 excludes the physical shell. If it
does not occur, every letter is `c`: the preceding upper letters, rule letter, and erasure tail
reduce to the complete all-`c` theorem MM-S65.

`rightmostRule_rawHead_shell_impossible` carries this split without changing the physical
target, including the marker-count contribution in the b-bearing branch. The all-`c` branch
proves that contribution is zero before invoking its specialized theorem.

## Boundary

The theorem covers every block containing a rule and followed only by erasures after its
rightmost rule. It does not cover all-erasure blocks, generalized later carriers, or singleton
targets.

## Verification

`MatrixMortality/DecimalSetterRuleEntry.lean` contains the exhaustive split and physical
composition. The narrow and root builds, namespace lint, selected transitive axiom checks,
forbidden-form scan, and diff check pass without warnings, suppressions, or proof apertures.

## Artifacts

- [`DecimalSetterRuleEntry.lean`](../MatrixMortality/DecimalSetterRuleEntry.lean)
- [`SALVAGE.md`](../SALVAGE.md#mm-s66-complete-rule-bearing-first-entry-extinction)

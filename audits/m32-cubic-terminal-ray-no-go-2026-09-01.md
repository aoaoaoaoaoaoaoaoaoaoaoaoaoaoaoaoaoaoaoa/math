# M₃(2) Fixed-Connector Transverse-Reader No-Go Audit

Date: 2026-09-01

Auditor and formalizer: GPT-5.6 Sol

Human role: elicited by @eternalism_4eva

## Enemy

`R32-S76` makes the free transverse source memory observable. The most direct next move is to
build positive projective inverses of the two transverse pump letters from the existing terminal
writer-reader-translation machinery and transport them through the established common-ray
connector. This audit decides that route exactly.

## Grammar

The formal segmented grammar permits arbitrary interleavings of:

- either radix writer;
- either positive radix reader;
- an arbitrary nonnegative count vector of all four positive terminal-translation words.

Lean proves positivity and exact projective realization of every flattened program. Its terminal
matrix is upper triangular with diagonal entries `((4/25)^h,1)` for a signed height `h∈ℤ`.

## Valuation Wall

The fixed connector with head `[1,15,8]` and tail `[13]` transforms the terminal diagonal ratio
into

```text
R = (9/340)(25/4)^h.
```

Since `v₁₇(9/340)=-1` and `v₁₇(25/4)=0`, every such `R` has valuation `-1`. The inverse false
and true transverse pumps require ratios `625` and `336000/197`; both have valuation zero.
Consequently neither ratio occurs. Lean strengthens this entry comparison to the matrix claim
that no transported grammar product times either pump is projectively scalar.

## Adjudication

| Claim | Judgment |
| --- | --- |
| segmented terminal grammar and positive flattening | Lean checked |
| physical projective realization | Lean checked |
| diagonal height normal form | Lean checked |
| transported `17`-adic valuation exactly `-1` | Lean checked |
| exclusion of both inverse ratios | Lean checked |
| direct projective-inverse exclusion | Lean checked |
| alternative ray connectors excluded | no |
| arbitrary positive raw waits excluded | no |
| `M₃(2)` is decided | open |

## Scope

The no-go belongs to the fixed `[1,15,8]`/`[13]` connector. It must not be promoted into a
statement about every positive connector: known alternative tails mapping `(4,3)ᵀ` to the
terminal ray have connector multipliers `21/8000`, `9/64`, `9/50`, and `9/100`, all of
`17`-adic valuation zero.

## Wound

```text
MASTER VERDICT: M₃(2) remains open
GAINED: an exact all-word obstruction for the current terminal/fixed-connector reader grammar
KILLED: further bounded searches for a transverse inverse behind that connector
EXPOSED: connector choice, rather than translation shift, as the missing inverse degree of freedom
NEXT: use the exact non-inverse height gate or search genuinely different connector architectures
```

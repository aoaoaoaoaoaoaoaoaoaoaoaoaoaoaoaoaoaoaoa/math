# M₃(2) Cubic Singleton-Selector Audit

Date: 2026-09-01

Auditor and formalizer: GPT-5.6 Sol

Human role: elicited by @eternalism_4eva

## Enemy

`R32-S68` proves that the free binary pump acts projectively faithfully on the physical
separator source. `R32-S69` supplies signed common-ray translations with positive physical
spellings. The unresolved local obligation is an exact positive left row annihilating one
selected encoded source and no other encoded source.

## Exact Synthesis

For `B=[[4,1],[3,1]]`, the prefix `A=(26,2,5,15)` satisfies

```text
rΠ(A)B = −2905210800·[1,937/3321].
```

The false-bit terminal translations

```text
N=[[1,−189665/144],[0,1]],
P=[[1,     2839/108],[0,1]]
```

obey

```text
N^6484 P^310371 = [[1,−762919/2],[0,1]].
```

The `R32-S69` connector transports this translation to

```text
F=[[1,−85828079/1020],[0,9/340]],
```

and exact rational arithmetic gives

```text
[1,937/3321]F=[1,−31049567/369]=[1,s(00)].
```

Every equality is a Lean theorem. The physical repetitions remain compressed; no proof step
expands their millions of waits.

## Converse

For any target `β` and candidate `γ`, Lean proves

```text
[1,s(β)] · Π(normalized loops,γ)(11,−123) = 0  iff  γ=β.
```

The lower source coordinate is nonzero. Zero incidence therefore equates the candidate's
projective coordinate with `s(β)`, and the source-coordinate injection from `R32-S68` recovers
the complete bit string. Exact basis pairing and the nonzero physical scale products transfer
this converse to the original physical source and row.

## Physical Selector

Let

```text
L=A ++ (1,15,8) ++ Nword^6484 ++ Pword^310371 ++ (13),
```

where `Nword` and `Pword` are the positive physical terminal blocks. Lean proves

```text
r Π(L ++ E(γ)) c = 0  iff  γ=00.
```

It also proves every wait in `L` is positive and computes the compressed length
`|L|=9,531,594`.

## Adjudication

| Claim | Judgment |
| --- | --- |
| prefix row coordinate | Lean checked |
| exact nonnegative translation synthesis | Lean checked |
| connector and selected target row | Lean checked |
| generic normalized singleton converse | Lean checked |
| physical incidence factorization | Lean checked |
| positivity and exact compressed length | Lean checked |
| physical `00`-selector biconditional | Lean checked |
| arbitrary raw-word converse | open |
| dynamic accepting computation compiler | open |
| `M₃(2)` is decided | open |

## Wound

```text
MASTER VERDICT: M₃(2) remains open
GAINED: a finite positive left word selecting exactly one encoded binary source
KILLED: nonexistence of a positive target annihilator as the source-stack obstruction
EXPOSED: dynamic arrival at the fixed target and arbitrary-raw-word grammar enforcement
NEXT: turn the singleton selector into a complete accepting compiler with a converse
```

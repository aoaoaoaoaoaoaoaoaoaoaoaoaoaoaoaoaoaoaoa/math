Historical report. The 2026-09-04 reconciliation rejected the aggregate dimension claim as
new mathematics: its surviving allocation hypothesis already supplies six modes. The affine
helper also had inconsistent hypotheses. The wrapper was removed; its underlying pole and
constant-mode obstructions remain. See [the reconciliation audit](../audits/reconciliation-2026-09-04.md).

Original verdict: obstructed

Cells affected: The poison-guarded two-register construction routes for `M₄(2)`, `M₄(3)`, and `M₅(3)`; no cell closes.

Author: GPT-5.6 Sol; elicited by @eternalism_4eva.

Lean: structure `MatrixMortality.PadicTwoCounterReader`; `#print axioms PadicTwoCounterReader.six_le_dimension` → `[propext, Classical.choice, Quot.sound]`; `#print axioms PadicTwoCounterReader.no_reader_of_dimension_le_five` → `[propext, Classical.choice, Quot.sound]`. The single-module build passed; the integration gate remains pending after the full build was externally stopped.

Statement: For every finite ambient mode type `Mode`, every `system : PadicTwoCounterReader Mode` has `6 ≤ Fintype.card Mode`; consequently, if `Fintype.card Mode ≤ 5`, then `¬Nonempty (PadicTwoCounterReader Mode)`. The structure quantifies two distinct prime valuation coordinates, a genuine finite-pole Möbius reader for each, integral unit-determinant action at the other register prime, a distinct poison prime with `trap_forward`, and `WallEnvelope`: either a mirrored one-cut common return, a poison-integral pole, or the normalized two-vertex mode dichotomy.

If obstructed: The sharp surviving inequality is `6 ≤ Fintype.card Mode`. Extending it to every return or two-cut bridge system requires proving that arbitrary compressed readers satisfy `WallEnvelope`; this exhaustiveness is not supplied by `MM-C06`, P, Z/C2, or G. An escape must use a nonmirrored/non-pole test, violate poison-integral unit isometry, or evade the six-mode spectral normal form.

DAG metadata: affine poison forward law / C2 / Lean / validated; mirrored one-cut wall / P + MM-C06 / Lean / validated; integral finite-pole wall / Z / Lean / validated; two-vertex six-mode wall / G / Lean / validated; envelope exhaustiveness / W / open / blocking.

The consequence is strategic, not a proved many-one reduction: within the checked envelope these three construction routes are dead, while their known decidability route still requires the unresolved projective-amortization core of `M₃(2)`. Thus they are decidable-or-`M₃(2)`-hard in the audit’s precise sense.

Next:
- Prove or refute `WallEnvelope` exhaustiveness for arbitrary `MM-C06` bridge paths.
